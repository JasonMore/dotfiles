#!/usr/bin/env bash
# Test mock for the `npx skills add` commands run by the installer.
set -euo pipefail
if [[ "${MOCK_NPX_FAIL:-0}" == "1" ]]; then
	echo "[mock-npx] simulated failure for: $*" >&2
	exit 1
fi
echo "[mock-npx] $*"
exit 0
